# Debug snapshots regenerated on every fetch (master_*.json is the source of truth)
raw_*.json
__pycache__/
*.pyc
.DS_Store
# Large generated dashboard -- kept out of git to avoid repo bloat; served via Netlify + artifact
crl_opponent_scout.html
# Transient build outputs (regenerated every run; NOT the persistent run_state.json)
run_summary.txt
discord_payload.json
_site/
# staging archive used to seed the repo (safe to delete)
code_update.tar.gz
code_update.zip
netlify_gate.tar.gz
feature_update.tar.gz
*.tar.gz
# self-contained (inline-icon) build produced only for the workflow artifact
crl_opponent_scout_standalone.html
crl_opponent_scout_standalone.html.gz

# Separate personal side-project (Chaos Draft), deliberately NOT part of the CRL tracker
_chaos_draft/

# Excel lock files (created while a workbook is open, deleted on close)
~$*

# Season archive of the Chaos Draft side-project (keep locally, do not commit)
chaos_draft_archive_*.tar.gz

# Chaos Draft — retired 2026-08-18, see chaos_draft_archive_final_*.tar.gz
chaos/
_duels_cache.pkl
matchup_database.html
matchup_database.html
matchup_database.html
